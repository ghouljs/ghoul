{
  "scripts": {
    "build": "rimraf -f lib && tsc",
    "deploy:example": "gh-pages -d example/gh-pages"
  },
  "devDependencies": {
    "gh-pages": "^1.0.0",
    "rimraf": "^2.6.1",
    "typescript": "^2.5.2"
  },
  "dependencies": {
    "whatwg-fetch": "^2.0.3"
  }
}
